// @ts-nocheck
/*

Your 2nd task:

Try to run this code using 
- JS Repl: Run

Fix errors in code, exmplain them

*/

const hello = "Hello";
const inJS = "in JavaScript";

// 2.2 show `Hello in JavaScript` on the screen

// 2.1 fix this error to see

let inJS = `sample (${10 + 10 + 2}) number`;

console.log(inJS);
